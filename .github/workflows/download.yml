name: Download via URL

on:
  push:
    branches:
      - main

jobs:
  download:
    runs-on: ubuntu-latest

    steps:
      - name: Download File
        run: |
          FILE_URL="http://m3u4u.com/epg/476rnmg5m8t29e1qnekg"
          FILE_NAME="epg.xml.gz"
          curl -LJO $FILE_URL
          mv $FILE_NAME .ebonini/epg/$FILE_NAME
