name: Atualização EPG 1

on:
  push:
    branches:
      - main
jobs:
  wget:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2
      - name: Create Directory
        run: mkdir -p ebonini/epg/epgauto
      - name: Download using wget
        run: wget -O ebonini/epg/epgauto/sample.html https://httpbin.org/html
